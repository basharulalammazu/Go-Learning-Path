# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out
coverage.txt

# Dependency directories (sometimes created by editors/tools)
vendor/

# IDE/editor settings
.vscode/
.idea/
*.swp

# Go modules: keep go.mod and go.sum tracked (do NOT ignore go.sum)
# If you intentionally vendor, you may remove vendor/ from ignore above

# Local environment files
.env
